
<h2>Book detail</h2>

<table class="table">
  <tbody>
    <tr>
      <td><%= @book.user.name %></td>
      <td><%= @book.title %></td>
      <td><%= @book.body %></td>
      <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
      <td>destroy</td>
    </tr>
  </tbody>
</table>